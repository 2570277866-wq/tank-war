# 坦克大战——双人联机对战游戏 课程设计报告

---

## 一、选题背景

### 1.1 课题来源

坦克大战（Battle City）是经典的FC平台游戏，其核心玩法——坦克对战、障碍物策略、实时对抗——至今仍具有很高的娱乐价值和设计参考意义。随着网络技术的发展，将经典单机玩法扩展为联机对战模式，不仅能够还原经典体验，还能增加玩家间的实时互动与竞技性。

本课题选择"双人联机坦克大战"作为课程设计题目，旨在综合运用C++面向对象编程、Windows网络编程、多线程同步、图形界面开发等技术，完成一个具备完整游戏流程、网络联机、服务端权威判定的实时对战游戏。

### 1.2 选题意义

1. **技术综合性强**：项目涵盖C++类体系设计、WinSock2 TCP网络通信、多线程并发控制、EasyX图形渲染等多个技术方向，是对课程所学知识的综合实践。
2. **架构设计典型**：采用客户端-服务端（C/S）架构，服务端权威判定、客户端预测与插值，是网络游戏开发的经典模式，具有工程参考价值。
3. **实时同步挑战**：20Hz状态快照广播、客户端插值平滑、断线重连等机制的实现，涉及实时系统设计的核心问题。
4. **团队协作实践**：双人分工开发、协议联调、Git版本管理，模拟真实项目协作流程。

### 1.3 国内外研究现状

实时对战游戏在网络游戏领域已有成熟方案，如帧同步（Lockstep）和状态同步（State Synchronization）两种主流架构。本项目采用状态同步方案，服务端以20Hz频率广播游戏快照，客户端进行插值渲染，在局域网环境下可达到流畅的对战体验。该方案实现复杂度适中，适合课程设计规模。

---

## 二、系统设计

### 2.1 系统总体架构

系统采用C/S架构，分为客户端和服务端两大模块：

```
┌──────────────┐         TCP          ┌──────────────┐
│   客户端 A   │◄──────────────────►│              │
│  (EasyX渲染) │    9527端口        │   TCP服务端   │
├──────────────┤                    │  (权威判定)   │
│   客户端 B   │◄──────────────────►│              │
│  (EasyX渲染) │                    └──────────────┘
└──────────────┘
```

**核心设计原则：**
- **服务端权威**：所有伤害判定、碰撞检测由服务端执行，客户端仅负责渲染和上报输入
- **状态同步**：服务端每50ms广播一次完整游戏快照，客户端按快照更新本地状态
- **客户端预测**：客户端在等待快照期间先本地预测移动，收到快照后修正

### 2.2 功能模块划分

#### 客户端模块（同学A负责）

| 模块 | 子模块 | 功能 |
|------|--------|------|
| 界面层 (UI) | MenuUI | 主菜单、坦克选择界面 |
| | LoginUI | 登录/注册界面 |
| | WaitUI | 等待匹配界面 |
| | GameOverUI | 结算界面 |
| | Minimap | 小地图 |
| 实体层 (Entity) | Tank | 坦克基类 + 3种派生类 |
| | Bullet | 子弹实体 |
| 核心层 (Core) | GameLoop | 游戏主循环（更新+渲染） |
| | InputHandler | 键盘输入处理 |
| | StateInterpolator | 客户端插值平滑 |
| 特效层 (Effect) | ParticleSystem | 粒子特效（爆炸/伤害飘字） |
| | GameMap | 地图系统（砖墙/钢墙/草丛/水域） |
| | Collision | 碰撞检测 |
| | DeathAnimator | 死亡动画 |
| 网络层 (Net) | NetClient | TCP客户端（连接/收发/分包） |
| | MsgCodec | 消息序列化/反序列化 |

#### 服务端模块（同学B负责）

| 模块 | 子模块 | 功能 |
|------|--------|------|
| 网络层 (Net) | TCPServer | TCP监听、Accept循环、Tick线程 |
| | Session | 客户端会话、消息分发 |
| 游戏层 (Game) | Room | 房间管理、匹配、断线重连 |
| | GameWorld | 游戏世界Tick、碰撞判定、快照打包 |
| | Judge | 碰撞工具函数 |
| 数据层 (Data) | UserManager | 用户注册/登录/密码哈希 |
| 核心层 (Core) | Clock | 高精度计时器 |

### 2.3 坦克类体系设计

采用工厂模式 + 多态设计：

```
Tank (抽象基类)
 ├── HeavyTank  — 重型坦克
 │   HP:200  速度:慢  伤害:大
 │   技能:铁壁护盾(3秒免伤, CD:15s)
 ├── LightTank  — 轻型坦克
 │   HP:150  速度:中  伤害:中
 │   技能:涡轮冲刺(2秒双倍速, CD:12s)
 └── ScoutTank  — 侦察坦克
     HP:100  速度:快  伤害:小
     技能:弹幕散射(3颗扇形弹, CD:10s)
```

基类定义纯虚接口（getDamage/getSpeed/getSkillType等），子类通过override返回不同属性值，工厂方法 `Tank::create()` 根据TankType创建对应子类实例。

### 2.4 网络协议设计

#### 消息格式

```
[2字节 MsgID] [2字节 bodyLen] [body变长]
```

#### 消息分类

| 消息ID范围 | 分类 | 方向 |
|-----------|------|------|
| 0x01xx | 认证 | 客户端→服务端 / 服务端→客户端 |
| 0x02xx | 房间 | 双向 |
| 0x03xx | 游戏同步 | 双向 |
| 0x04xx | 结算 | 双向 |
| 0x05xx | 心跳 | 双向 |

#### 同步流程

```
客户端                    服务端                    客户端
  │──C2S_INPUT(按键)──→│                         │
  │                     │──逻辑Tick(50ms)──→     │
  │←─S2C_SNAPSHOT─────│──S2C_SNAPSHOT──→       │
  │  插值渲染           │                         │
```

### 2.5 地图系统设计

| 障碍类型 | 属性 | 碰撞 | 子弹交互 |
|---------|------|------|---------|
| 砖墙 | 可破坏, HP=2 | 阻挡坦克和子弹 | 子弹命中减HP，HP=0消失 |
| 钢墙 | 不可破坏 | 阻挡坦克和子弹 | 子弹命中消失，墙不损 |
| 草丛 | 隐身效果 | 不阻挡 | 子弹穿过 |
| 水域 | 不可通行 | 阻挡坦克 | 子弹穿过 |

### 2.6 断线重连机制

1. 服务端心跳检测：3秒无心跳标记为断线
2. 断线后游戏暂停（RoomState::PAUSED），通知双方
3. 10秒重连窗口期内，客户端发送C2S_RECONNECT可恢复
4. 超时未重连，断线方判负

---

## 三、系统实现

### 3.1 开发环境

| 项目 | 配置 |
|------|------|
| 编程语言 | C++20 |
| IDE | Visual Studio 2022 |
| 图形库 | EasyX 2023 |
| 网络库 | WinSock2 |
| 版本管理 | Git + GitHub |
| 编译器 | MSVC (x64) |

### 3.2 客户端关键实现

#### 3.2.1 游戏主循环

```cpp
void GameLoop::update(float dt) {
    m_input.update();              // 读取键盘
    InputState input = m_input.getState();
    m_localTank->applyInput(input); // 本地预测移动
    m_localTank->update(dt);        // 更新技能CD等
    if (spacePressed) fireBullet(); // 射击
    if (fPressed) m_localTank->useSkill(); // 技能
    updateBullets(dt);              // 子弹移动
    checkCollisions();              // 碰撞检测
    m_particles.update(dt);         // 粒子特效更新
}
```

#### 3.2.2 快照同步

```cpp
void GameLoop::applySnapshot(const Snapshot& snap) {
    if (snap.frameSeq <= m_lastSnapSeq) return; // 丢弃旧快照
    m_lastSnapSeq = snap.frameSeq;
    for (int i = 0; i < 2; i++) {
        Tank* tank = (snap.tanks[i].playerID == 0)
                     ? m_localTank.get() : m_enemyTank.get();
        tank->setPosFromSnapshot(snap.tanks[i]); // 服务端修正
    }
    // 重建子弹列表
    m_bullets.clear();
    for (int i = 0; i < snap.bulletCount; i++)
        m_bullets.emplace_back(
            make_unique<Bullet>(snap.bullets[i]));
}
```

#### 3.2.3 粒子特效系统

粒子系统支持4种特效：
- **爆炸粒子**：命中时从命中点向四周发射12个粒子，带衰减和重力
- **伤害飘字**：命中时显示红色"−30"文字，向上飘浮1秒后消失
- **护盾粒子**：重型坦克开盾时在周围发射蓝色粒子环
- **冲刺拖尾**：轻型坦克冲刺时在尾部生成黄色拖尾粒子

#### 3.2.4 坦克渲染

坦克绘制采用旋转矩形+圆形炮塔+线段炮管的组合方式，通过旋转矩阵计算四个顶点坐标后调用 `fillpolygon` 绘制车身，`fillcircle` 绘制炮塔，`line` 绘制炮管。

### 3.3 服务端关键实现

#### 3.3.1 游戏Tick循环

服务端以20Hz频率执行GameWorld::Tick()，每次Tick执行：
1. 读取两个客户端的输入
2. 移动坦克（根据输入和速度属性）
3. 处理射击请求（生成子弹）
4. 处理技能请求（激活护盾/冲刺/散射）
5. 移动所有子弹
6. 碰撞检测（子弹-坦克圆形碰撞）
7. 伤害计算（考虑护盾免伤）
8. 游戏结束判定
9. 打包快照并广播

#### 3.3.2 房间匹配

```cpp
Room* TCPServer::FindOrCreateRoom() {
    for (auto* room : rooms)
        if (!room->IsFull()) return room;  // 找未满房间
    rooms.push_back(new Room());           // 没有则新建
    return rooms.back();
}
```

#### 3.3.3 用户数据管理

用户数据以文本文件 `users.dat` 持久化存储，每行格式：
```
username|password_hash|wins|losses|kills
```
密码使用DJB2哈希算法存储，注册时检查用户名唯一性，登录时比对哈希值。

#### 3.3.4 心跳与断线处理

- Session记录最后心跳时间戳（微秒级，QueryPerformanceCounter）
- GameTickLoop中检查心跳超时（3秒）
- 超时标记disconnectPending，触发HandleDisconnect
- 游戏暂停，10秒内可重连，超时判负

### 3.4 协作流程

#### Git分支策略

| 分支 | 用途 |
|------|------|
| main | 稳定版本，每周合并 |
| dev/client | 客户端开发 |
| dev/server | 服务端开发 |
| dev/protocol | 协议变更 |

#### 开发时间线

| 周次 | 客户端交付 | 服务端交付 |
|------|-----------|-----------|
| W1 | EasyX框架+主菜单+选坦克 | TCP服务端+Accept+Session |
| W2 | 登录UI+NetClient+MsgCodec | UserManager+Room+协议修复 |
| W3 | 粒子特效+单人可玩 | GameWorld+Tick+碰撞判定 |
| W4 | 快照同步+客户端预测 | 快照广播+心跳+断线重连 |
| W5 | 地图系统+等待界面+结算 | 战绩保存+多房间 |
| W6 | 插值平滑+死亡动画+小地图 | 性能优化+防作弊 |
| W7 | 集成测试+修复 | 集成测试+修复 |

---

## 四、结果分析

### 4.1 功能测试结果

| 测试项 | 预期结果 | 实际结果 | 状态 |
|--------|---------|---------|------|
| 主菜单显示 | 显示标题和两个按钮 | 正常显示 | 通过 |
| 登录注册 | 输入账号密码，服务端存储验证 | 正常登录注册 | 通过 |
| 坦克选择 | 三种坦克可选，属性显示正确 | 正常选择 | 通过 |
| 房间匹配 | 两人加入自动开始 | 匹配成功 | 通过 |
| WASD移动 | 坦克按方向移动 | 移动流畅 | 通过 |
| 空格射击 | 子弹沿朝向发射 | 正常发射 | 通过 |
| F技能释放 | 各坦克技能效果正确 | 护盾/冲刺/散射正常 | 通过 |
| 碰撞检测 | 子弹命中扣血，墙壁阻挡 | 判定准确 | 通过 |
| 地图交互 | 砖墙可破坏，钢墙不可，草丛隐身 | 交互正确 | 通过 |
| 断线重连 | 断线后暂停，重连恢复 | 重连成功 | 通过 |
| 结算界面 | 显示胜负和战绩 | 正常显示 | 通过 |

### 4.2 性能测试

| 指标 | 数值 |
|------|------|
| 服务端Tick频率 | 20Hz（50ms/帧） |
| 客户端渲染帧率 | 60FPS |
| 快照大小 | 约2KB（含2坦克+64子弹槽） |
| 单房间内存占用 | <1MB |
| 局域网延迟 | <5ms |
| 心跳超时阈值 | 3秒 |
| 重连等待时间 | 10秒 |

### 4.3 系统稳定性

- 服务端可长时间运行，无内存泄漏
- 多房间可同时运行，互不干扰
- 客户端异常断开后服务端正确清理资源
- 快照序列号机制防止旧包导致状态回退

### 4.4 不足与改进方向

1. **子弹速度单位不一致**：初期客户端子弹用帧速移动而服务端用dt移动，已统一为帧速
2. **协议结构体对齐**：TankState曾缺少shootTimer字段导致sizeof不同，快照反序列化错位，已同步修复
3. **无敌方AI**：单人模式下敌方坦克静止，可后续加入简单AI
4. **无音效**：射击和爆炸无声音反馈，可加入PlaySound
5. **地图固定**：当前地图硬编码，可改为文件配置
6. **无加密**：网络通信明文传输，可加入简单加密

---

## 五、课程设计总结

### 5.1 技术收获

通过本次课程设计，我们在以下方面获得了深入实践：

1. **C++面向对象设计**：通过坦克类体系的设计，深入理解了继承、多态、工厂模式的实际应用。三种坦克通过override返回不同属性值，实现了运行时多态行为。

2. **网络编程**：掌握了WinSock2 TCP通信的完整流程，包括服务端监听、客户端连接、消息分包处理、多线程收发。理解了TCP粘包问题及解决方案（定长头+变长体）。

3. **多线程并发**：服务端使用多线程处理多个客户端连接，通过mutex保护共享数据，使用atomic标志处理断线状态，避免了竞态条件。

4. **实时同步架构**：实现了状态同步模式，服务端权威判定+客户端预测插值，理解了帧同步与状态同步的区别与适用场景。

5. **图形渲染**：通过EasyX库实现了完整的游戏UI和渲染系统，包括旋转多边形绘制、粒子系统、批量绘制优化。

### 5.2 团队协作经验

1. **协议先行**：Common/Protocol.h是整个项目的契约，任何修改必须双方确认。项目中曾因TankState结构体不一致导致快照反序列化错位，深刻体会到协议一致性的重要性。

2. **分支管理**：采用dev/client和dev/server分支独立开发，定期合并到main。通过GitHub进行代码同步，避免了直接修改对方代码的混乱。

3. **联调流程**：先各自开发并通过单元测试，再进行端到端联调。服务端先启动，客户端后连接，按消息类型逐步验证。

### 5.3 个人体会

本次课程设计将课堂所学的C++编程、数据结构、操作系统、计算机网络等知识融会贯通，完成了一个从零到一可运行的网络对战游戏。项目过程中遇到了协议不一致、编译路径配置、编码格式等诸多问题，通过查阅资料和团队协作逐一解决，提升了问题定位和调试能力。

双人联机坦克大战虽规模不大，但涵盖了网络游戏的典型架构和核心机制，为今后从事游戏开发或分布式系统开发打下了实践基础。
